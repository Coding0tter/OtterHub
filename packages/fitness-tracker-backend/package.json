{
  "name": "fitness-tracker-backend",
  "module": "server.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile ../../output/fitness-tracker-backend server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "elysia": "^1.2.25",
    "mongoose": "^8.12.1",
    "shared": "workspace:*",
    "auth": "workspace:*"
  }
}
